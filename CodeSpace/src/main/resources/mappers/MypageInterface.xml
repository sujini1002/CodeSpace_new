<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team.cos.mypage.dao.MypageInterface">

<select id="getUserInfo" resultType="com.team.cos.userinfo.vo.UserInfoVo">
	select * from userinfo where user_id = #{user_id}
</select>

<update id="update" parameterType="com.team.cos.userinfo.vo.UserInfoVo">
	update userinfo set
	user_photo = #{user_photo},
	user_url = #{user_url},
	user_intro = #{user_intro},
	user_tag = #{user_tag}
	where user_id = #{user_id}
</update>

<delete id="memberDelete">
	delete from userinfo where user_id = #{user_id}
</delete>

<!-- 내가 한 질문 얻어오기 -->
<select id="getMyQst" resultType="com.team.cos.question.vo.QuestionInfo">
	select * from question where user_no = #{user_no} order by q_regdate desc
</select>

<!-- 내가 한 답변 얻어오기 -->
<!-- <select id="getMyAns" resultType="com.team.cos.question.vo.QuestionInfo">

</select>
 -->

</mapper>